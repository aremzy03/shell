#include <stdio.h>
#include <string.h>
int main(void)
{
	char str[] = "Aremzy-Boss";
	char* token;

	token  = strtok(str, " ");
	while(token != NULL)
	{
		printf("%s\n", token);
		token = strtok(NULL, " ");
	}
	return 0;
}
