#include <stdio.h>
int main(void)
{
	int n;
	char line[100];

	n = getline(line, sizeof(line), stdin);
	if(n == -1)
	{
		printf("error");
		retutn(1);
	}
	else
	{
		printf("%s", line);
	}
	return (1);
}
